import { Setup } from '../Setup';
export declare class AuthenticationSetup {
    setup: Setup;
    onAuthenticationRequired: any;
    persist: boolean;
    constructor(setup: Setup, options?: any);
}
